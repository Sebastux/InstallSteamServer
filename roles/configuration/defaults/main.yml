---
liste_install: [ 'aptitude', 'neofetch' ]

ssh_port: 22

nom: "{{ nom_machine | default('debian11','debian11') }}"
domaine: "{{ domaine_machine | default('localhost.localdomain','localhost.localdomain') }}"

fqdn_machine: "{{ nom }}.{{ domaine }}"
